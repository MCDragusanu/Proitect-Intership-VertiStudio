import React from "react";
import "./spinner.css"; // Assuming you'll have this CSS file for styles

const Spinner: React.FC = () => {
	return <div className="spinner" />;
};

export default Spinner;
